= form_tag search_path, method: :get do
    = select_tag :scope, options_for_select(SearchService::TYPES, params[:scope])
    = text_field_tag :query, params[:query], placeholder: 'Search for:'
    = submit_tag 'Search'
  